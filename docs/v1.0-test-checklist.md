# v1.0.0 Release-blocker Lifecycle Linux 测试清单

测试时禁用 MultiSlots、CreateSurvivorBot / `l4d_CreateSurvivorBot`、Left4DHooks takeover 功能和旧 AFK dead-bot fix；保持 Valve `survivor_limit 4`。每项同时检查 SourceMod error log 中的 Population / Identity / Session 结构化日志。

## A. Baseline Population

- [x] 1. 干净 restart / changelevel，`sm_l4dp_min_survivors 4`、`sm_l4dp_survivor_limit 4`；第一名真人进入后自动成为 Survivor，并同时存在另外 3 个正常 Survivor Bot。
- [x] 2. 连续 changelevel 测试中 baseline 仍为 1 真人 + 3 Bot，未额外重复生成 Bot。
- [ ] 3. 踢光 Bot 后 changelevel 的专门场景未单独记录；需要后续回归。
- [x] baseline 为有界回合初始化 reconcile，不存在每秒 population Timer（已通过静态验证）。
- [x] 第一名真人 Auto Join 在 baseline 建立后正常接管 Survivor Bot。

## B. Identity transfer 与 Idle transaction

- [x] 4. 连续执行 `!afk` → 确认 Engine Idle → `!join`；绑定、接回和 character/model/第一人称手臂正常。
- [x] 5. 连续完成 Auto Idle → `!join`；Auto Idle 正确进入 Engine Idle，不再掉成 Free Spectator。
- [x] 6. Free Spectator 通过 `!spec` / `!join` 正常离开和重新接管 Survivor。
- [x] 7. 无 existing Bot 时通过 `!join` / Auto Join 创建真正 5+ Survivor；identity takeover、模型/手臂和 mid-join loadout 已验证正常。
- [x] human↔Bot Identity Lifecycle 实机转换正常，新建 Survivor takeover 不再出现感染者模型/第一人称手臂。
- [ ] newly-created 5+ 的安全位置所有失败分支未单独覆盖。
- [ ] 人为制造 Idle 验证超时的 rollback 路径未单独覆盖。
- [ ] 若回滚本身失败，`FATAL idle rollback failed` 的实机 fatal 分支尚未人为制造。

## C. 5/8/12/16 与角色

- [x] 8. root 使用 `sm_l4dp_addbot` 分别补到 5 / 8 / 12 / 16 Survivors；已完成 1 真人 + Bots 的总 Survivor 数量实机验证。
- [x] 新建 Bot 的 identity、存活与基本 AI 行为在上述单真人测试中正常。
- [ ] 未覆盖：5+ 真人同时在线，以及 8/12/16 真人并发加入。
- [x] `!csm` 切换角色实机正常。

## D. Human Team Wipe 和状态隔离

- [x] 9. 触发 Human Team Wipe 的团灭与不团灭正负边界已完成实机验证，Population Manager 未破坏团灭流程。
- [x] 真人只是倒地/挂边时不团灭；Engine Idle 的绑定 Bot 存活时仍计为有效存活真人。
- [x] 10. `!spec` 是 Free Spectator，`!spec` / `!join` 状态转换已实机验证正常。

## E. 个人夜视仪

- [x] `!ysy` 打开夜视仪子菜单；`!p` →“夜视仪”打开相同菜单，开关状态显示正常。
- [x] 双击 F 打开/关闭 Players 夜视菜单实机正常。
- [x] 单击 F 的原版手电筒正常；双击检测不消费 impulse 100。
- [x] 首次开启默认 2 档，distance = 250；1 → 8 每档增加 125，8 档为 1000，加档/减档边界正常。
- [x] 光源 `_light` 为 `255 255 255 255`，纯白光和整体夜视行为实机正常。
- [ ] 未覆盖：两名真人客户端互相观察 `SDKHook_SetTransmit` owner-only 可见性隔离。
- [x] `!afk` / `!join` / `!spec`、死亡/复活和 changelevel 的夜视实体生命周期行为正常。
- [x] 调档与本次连接开关行为符合设计：重连后默认关闭，并恢复上次档位。
- [ ] 16 真人同时开启时，每人最多一个 entity reference 有效，且只收到自己的光源。

## F. 不回归验收

- [x] Auto Idle CenterText 和 Idle Kick HintText 行为正常；Idle Kick warning / kick 已实机验证。
- [x] `!afk` / `!join` / `!spec` / `!zs` / `!bhop` / `!csm` / `!ysy` 无回归；`!bhop` ClientPrefs 持久化正常。
- [ ] mid-join 安全位置失败时仍成功加入；melee 创建全部失败时 fallback pistol。
- [ ] 保持 `sb_all_bot_game 1`；所有真人 Engine Idle 时 Bot 仍继续游戏。

## 最终记录

- Windows 编译/静态/签名验证：由 `scripts/build.ps1` 和 `scripts/validate.ps1` 自动记录。
- Linux dedicated server：已完成核心功能和生命周期实机验证。
- 5/8/12/16：已完成总 Survivor 数量验证（1 真人 + Bots）。
- 未覆盖：5+ 真人并发、多真人 Night Vision owner-only 可见性，以及人为强制 Idle rollback fatal failure。这些项目未执行前不宣称实机通过。
