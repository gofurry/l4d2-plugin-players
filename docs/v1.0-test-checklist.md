# v1.0.0 Release-blocker Lifecycle Linux 测试清单

测试时禁用 MultiSlots、CreateSurvivorBot / `l4d_CreateSurvivorBot`、Left4DHooks takeover 功能和旧 AFK dead-bot fix；保持 Valve `survivor_limit 4`。每项同时检查 SourceMod error log 中的 Population / Identity / Session 结构化日志。

## A. Baseline Population

- [ ] 1. 干净 restart / changelevel，`sm_l4dp_min_survivors 4`、`sm_l4dp_survivor_limit 4`；第一名真人进入后自动成为 Survivor，并同时存在另外 3 个正常 Survivor Bot。
- [ ] 2. 连续 changelevel 至少 5 章；每章仍为 1 真人 + 3 Bot，不额外重复生成 Bot。
- [ ] 3. 踢光 Bot 后 changelevel；新回合重新建立 baseline 4，但在当前战斗中踢/杀 Bot 不会自动补员。
- [ ] baseline 日志只在回合初始化窗口出现，有 `before/after/baseline/attempt`；不存在每秒 population Timer。
- [ ] 第一名真人 Auto Join 必须等待 `Population reconcile succeeded`，然后接管 existing baseline Bot，不触发 mid-join loadout/placement。

## B. Identity transfer 与 Idle transaction

- [ ] 4. 连续执行 `!afk` → 确认 Engine Idle → `!join` 至少 10 次；每次都有正确绑定 Bot，永远不掉成 Free Spectator，character/model/第一人称手臂不变。
- [ ] 5. 连续完成 Auto Idle → `!join` 至少 10 次；行为与 manual `!afk` 一致，不得出现普通 spectator 结果。
- [ ] 6. Free Spectator 通过 `!join` 接管 existing Bot；人称模型和第一人称手臂正常，不触发 mid-join 位置/装备。
- [ ] 7. 无 existing Bot 时通过 `!join` / Auto Join 创建真正 5+ Survivor；首先完成合法 Survivor identity takeover，再应用 mid-join position/loadout，模型/手臂始终正常。
- [ ] 每次 human↔Bot 替换日志包含 `Identity player->bot`、`Identity bot->player` 和下一帧 `Identity verification succeeded`。
- [ ] 人为制造 Idle 验证超时时，日志显示 rollback 启动；回滚使用 existing takeover state machine，不 teleport、不换装备，最终恢复 Active Survivor。
- [ ] 若回滚本身失败，error log 必须包含 `FATAL idle rollback failed` 以及 stage/client/team/bot/identity，不能静默失败。

## C. 5/8/12/16 与角色

- [ ] 8. root 使用 `sm_l4dp_addbot` 分别补到 5 / 8 / 12 / 16 Survivors；所有 Bot 都是 Nick/Rochelle/Coach/Ellis 合法 identity，允许重复，AI 存活且可正常行动。
- [ ] 诊断 Bot 与 Join/Population 共用 `LP_CreateUnlimitedSurvivorBot()` 和 Identity initializer；无 client slot、SDKCall 失败或 identity 失败都有明确阶段日志。
- [ ] `!csm` 切换 8 个角色均正常；下一帧 Identity verifier 不会立即覆盖玩家主动选择。

## D. Human Team Wipe 和状态隔离

- [ ] 9. 触发 Human Team Wipe 时，Population Manager 不补 Bot；剩余普通 Bot 自杀后由 L4D2 自然判定 round failure。
- [ ] 真人只是倒地/挂边时不团灭；Engine Idle 的绑定 Bot 存活时仍计为有效存活真人。
- [ ] 10. `!spec` 是 Free Spectator，不保留 Idle 归属；长时间旁观不被 Players 踢出，主动 `!spec` 后 Auto Join 不再强制拉回，手动 `!join` 仍正常。

## E. 不回归验收

- [ ] Auto Idle CenterText 和 Idle Kick HintText 行为正常，无乱码/单独数字/残留；Idle Kick 仍能警告并踢出。
- [ ] `!afk` / `!join` / `!spec` / `!zs` / `!bhop` / `!csm` 无回归。
- [ ] mid-join 安全位置失败时仍成功加入；melee 创建全部失败时 fallback pistol。
- [ ] 保持 `sb_all_bot_game 1`；所有真人 Engine Idle 时 Bot 仍继续游戏。

## 最终记录

- Windows 编译/静态/签名验证：由 `scripts/build.ps1` 和 `scripts/validate.ps1` 自动记录。
- Linux 运行验证：待测试服按上述清单执行，未执行前不宣称实机通过。
- 5/8/12/16 人实机验证：待执行。
